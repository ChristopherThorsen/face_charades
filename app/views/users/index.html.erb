<div class="header">
  <div class="left-third">
    <%= button_to "New Round", [@card, @round] %>
  </div>
  <div class="middle">
    <h1> Friends </h1>
  </div>
  <div class="right-third">
    <%= link_to "+", :facebook_friends %>
  </div>
</div>
<ul>
  <% @friends.each do |friend| %>
    <li><%= link_to friend.name, friend %></li>
  <% end %>
</ul>
